# 部署倉位視覺驗收暫存紀錄

## 2026-07-29 自動預覽觀察

- 管理式預覽對 `/snapshots` 回報可完成擷取，但畫面內容為空白。
- 管理式預覽對 `/strategies` 以完整頁與首屏兩種模式皆回報擷取失敗。
- 直接開啟 `/strategies` 可取得正確網頁標題與預覽模式提示列，但 React 應用區沒有可見元素，瀏覽器主控台亦沒有前端例外。
- 文件已進入 `complete`，`/@vite/client` 與 `/src/main.tsx` 均出現在資源紀錄中，但 `#root` 仍為零子節點；手動重新匯入入口模組時瀏覽器回報 `Failed to fetch dynamically imported module`。這組證據較符合預覽代理或模組抓取工作階段異常，而不是部署倉位元件本身的可見 React 例外。
- 公開預覽入口模組連續回傳 HTTP `429 PROXY_RATE_LIMITED`，本機 `127.0.0.1:3000` 同一路徑則回傳 HTTP 200 與完整 Vite 模組；然而自動瀏覽器改走本機位址後仍只取得空白根節點，且畫面上沒有互動元素。因此目前自動視覺擷取通道不可作為此修復的可靠驗收來源。
- 現階段尚未把空白畫面判定為本輪部署倉位程式回歸；需繼續檢查 HTML 腳本、網路載入與預覽工作階段狀態。
- 本輪驗收不得建立、啟用策略或觸發真實訂單；資料流驗證採唯讀或可回復的程式級方法。
