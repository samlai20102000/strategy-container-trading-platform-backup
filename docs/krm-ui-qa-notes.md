# KRM V2 UI 驗證記錄

## 2026-08-01 桌面版

- 已以 1440 × 1100 完整頁面擷取 `/backtest`、`/strategies`、`/deployments`。
- 回測中心的共用分層馬丁區已正確顯示 V6.1 預設三段：L1–4 = 1.5、L5–9 = 1.1、L10–11 = 1.0，並顯示「新增分層」操作；最大層數自動計算為 11。
- 回測表單、策略卡片網格與三模式部署工作台在桌面寬度沒有水平溢出或文字／背景對比問題。
- 三模式部署工作台能正常載入既有部署；KRM 專用 H3 4% 提示需在選取 KRM deployment 且進入模式／政策編輯時才會出現。
- 獨立互動瀏覽器目前未登入，因此無法在該 session 切換策略下拉選單；管理預覽 session 已登入並能正常擷取完整頁面。

## 2026-08-01 行動版

已以 390 × 844 完整頁面擷取相同三條路由。回測表單改為單欄，V6.1 三段分層列、刪除操作與「新增分層」仍保留在卡片內；策略卡片依序堆疊；部署清單與詳細面板由左右欄改為上下排列。三頁均未出現頁面級水平溢出、截斷主操作或重疊文字。

## 2026-08-01 參數快照庫

桌面版可在 KRM 快照列直接辨識 `30m` 與 KAMA／層級摘要，列表統計欄對齊且無重疊。390px 行動版保留完整快照名稱，較寬的策略摘要與績效欄位位於表格橫向捲動區；頁面本身沒有產生額外水平溢出。
