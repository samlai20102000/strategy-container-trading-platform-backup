# SMA v3.00 EMA 馬丁策略 - 完整入場出場條件

## 策略概述

**策略名稱**：SMA v3.00 對稱統一版（EMA 交叉 + 網格馬丁 + 金額止盈）

**核心邏輯**：基於 EMA 快速線（Killer）與中速線（Wave）的交叉信號進行入場，使用階梯式馬丁加倉系統進行補倉，採用金額追踪止盈和動態風控進行出場。

---

## 第一部分：入場條件

### 1.1 交叉信號判斷

策略使用兩條 EMA 線進行交叉判斷：

| 參數 | 名稱 | 預設週期 | 用途 |
|------|------|--------|------|
| EMA1_Period | Killer（快速線） | 3 | 交叉判斷 |
| EMA2_Period | Wave（中速線） | 6 | 交叉判斷 |
| EMA3_Period | Enter（進入線） | 15 | 入場價格判斷 |
| EMA4_Period | Lower（下軌線） | 30 | 預留參數 |
| EMA5_Period | Upper（上軌線） | 60 | 預留參數 |

### 1.2 做多入場條件

**條件 1**：Killer 上穿 Wave
```
killerNow > waveNow AND killerPrev ≤ wavePrev
```

**條件 2**：當前價格 < Enter EMA
```
price < EMA3(15)
```

**觸發**：條件 1 AND 條件 2 同時滿足
```
→ 以當前價格開倉做多（第 1 層）
```

### 1.3 做空入場條件

**條件 1**：Killer 下穿 Wave
```
killerNow < waveNow AND killerPrev ≥ wavePrev
```

**條件 2**：當前價格 > Enter EMA
```
price > EMA3(15)
```

**觸發**：條件 1 AND 條件 2 同時滿足
```
→ 以當前價格開倉做空（第 1 層）
```

### 1.4 開倉風控檢查

開倉前檢查**持倉比例限制**：

```
開倉名義值 = 初始手數 × 當前價格
最大允許名義值 = 當前權益 × Max_Position_Ratio（預設 20%）

IF 開倉名義值 > 最大允許名義值
  → 拒絕開倉
ELSE
  → 執行開倉
```

---

## 第二部分：加倉條件（馬丁補倉迴圈）

### 2.1 網格加倉觸發條件

**條件**：價格偏離最後一層加倉點超過網格間距

#### 做多加倉
```
IF 當前價格 ≤ 最後加倉價格 - 網格間距
  → 觸發加倉
```

#### 做空加倉
```
IF 當前價格 ≥ 最後加倉價格 + 網格間距
  → 觸發加倉
```

### 2.2 網格間距計算（階梯式分層）

網格間距由 **Martin_Tiers**（階梯式分層表）決定：

```json
Martin_Tiers = [
  { start: 1, end: 4, multiplier: 1.5, pipstep: 50000 },
  { start: 5, end: 9, multiplier: 1.1, pipstep: 50000 },
  { start: 10, end: 11, multiplier: 1.0, pipstep: 50000 }
]
```

**計算公式**：
```
網格間距 = pipstep × Point_Value

例如：
  pipstep = 50000
  Point_Value = 100（BTC）
  網格間距 = 50000 × 100 = 5,000,000 點
```

### 2.3 加倉手數計算（階梯式累乘）

每層的手數根據階梯式分層表的乘數累乘計算：

```
第 1 層手數 = Base_Lot_Size（初始手數）
第 2 層手數 = 第 1 層手數 × Tier[1].multiplier
第 3 層手數 = 第 2 層手數 × Tier[2].multiplier
...
第 N 層手數 = 第 N-1 層手數 × Tier[N].multiplier
```

**例如**（預設參數）：
```
Base_Lot_Size = 0.01 BTC
Tier[1].multiplier = 1.5
Tier[2].multiplier = 1.5
Tier[3].multiplier = 1.5
Tier[4].multiplier = 1.5
Tier[5].multiplier = 1.1
...

第 1 層 = 0.01 BTC
第 2 層 = 0.01 × 1.5 = 0.015 BTC
第 3 層 = 0.015 × 1.5 = 0.0225 BTC
第 4 層 = 0.0225 × 1.5 = 0.03375 BTC
第 5 層 = 0.03375 × 1.1 = 0.037125 BTC
...
```

### 2.4 加倉風控檢查

加倉前檢查**持倉比例限制**：

```
加倉後總名義值 = (當前總手數 + 新層手數) × 當前價格
最大允許名義值 = 當前權益 × Max_Position_Ratio（預設 20%）

IF 加倉後總名義值 > 最大允許名義值
  → 拒絕加倉
ELSE
  → 執行加倉
```

### 2.5 加倉層數限制

```
IF 當前層數 ≥ MaxMartinLevels（預設 11 層）
  → 停止加倉
```

MaxMartinLevels 自動從 Martin_Tiers 的最後一層 end 值讀取。

---

## 第三部分：出場條件

### 3.1 金額追踪止盈（Priority 1）

**觸發條件**：

```
1. 浮盈達到目標金額
   IF 浮盈 ≥ Dollar_Start（預設 $8）
     → 激活止盈追踪

2. 從峰值回撤超過回撤金額
   IF 峰值浮盈 - 當前浮盈 ≥ Dollar_Trail（預設 $1.5）
     → 觸發止盈平倉
```

**做多止盈**：
```
目標金額 = Dollar_Start_Buy（預設 $8）
回撤金額 = Dollar_Trail（預設 $1.5）
```

**做空止盈**：
```
目標金額 = Dollar_Start_Sell（預設 $8）
回撤金額 = Dollar_Trail（預設 $1.5）
```

**出場記錄**：
```
exitReason = "止盈觸發（第 N 層，峰值 $X.XX，回撤 $Y.YY ≥ $Z.ZZ）"
```

### 3.2 硬止損（Priority 2）

**觸發條件**：

```
IF 浮虧金額 ≤ -Dollar_Loss（預設 -$100）
  → 立即平倉
```

**計算公式**：
```
浮虧金額 = (當前價格 - 均價) × 總手數（做多）
         = (均價 - 當前價格) × 總手數（做空）
```

**出場記錄**：
```
exitReason = "硬止損（浮虧 $-X.XX ≤ -$Y.YY）"
```

### 3.3 動態權益回撤止損（Priority 3）

**觸發條件**：

```
IF 當前權益回撤 ≥ Max_Equity_Drawdown（預設 5%）
  → 立即平倉
```

**計算公式**：
```
當前權益 = 基礎權益 + 浮盈
權益回撤 = (峰值權益 - 當前權益) / 峰值權益

IF 權益回撤 ≥ 0.05（5%）
  → 觸發止損
```

**出場記錄**：
```
exitReason = "動態回撤止損（回撤 X.XX% ≥ Y.Y%，峰值 $Z.ZZ）"
```

### 3.4 方向轉換（已停用）

**說明**：SMA v3.00 已完全移除 EMA 交叉時的平倉機制。

```
當 Killer 下穿 Wave（做多時）或上穿 Wave（做空時）
  → 不平倉，不轉向
  → 持倉保持不變，等待止盈/止損觸發
```

### 3.5 回測結束強制平倉

```
IF 回測結束 AND 仍有持倉
  → 以最後一根 K 線收盤價平倉
  exitReason = "回測結束強制平倉"
```

---

## 第四部分：完整交易流程

### 4.1 流程圖

```
開始回測
  ↓
計算 5 條 EMA 線
  ↓
[無持倉狀態]
  ↓
檢查入場信號（Killer 與 Wave 交叉）
  ↓
  ├─ 無信號 → 等待下一根 K 線
  │
  └─ 有信號 → 檢查價格與 Enter EMA 關係
       ↓
       ├─ 不符合 → 等待下一根 K 線
       │
       └─ 符合 → 檢查持倉比例風控
            ↓
            ├─ 超限 → 拒絕開倉
            │
            └─ 通過 → 開倉（第 1 層）
                 ↓
            [有持倉狀態]
                 ↓
            ┌─────────────────────────────────────┐
            │ 每根 K 線檢查以下條件（優先級順序）  │
            └─────────────────────────────────────┘
                 ↓
            1. 檢查權益回撤止損
               IF 回撤 ≥ 5% → 平倉（動態回撤止損）
                 ↓
            2. 檢查硬止損
               IF 浮虧 ≤ -$100 → 平倉（硬止損）
                 ↓
            3. 檢查金額追踪止盈
               IF 浮盈 ≥ $8 AND 回撤 ≥ $1.5 → 平倉（止盈觸發）
                 ↓
            4. 檢查網格加倉
               IF 價格偏離 > 網格間距 AND 層數 < 11
                  AND 持倉比例未超限
                  → 加倉（第 N+1 層）
                 ↓
            5. 無觸發條件
               → 持倉保持，等待下一根 K 線
                 ↓
            [平倉後返回無持倉狀態]
                 ↓
回測結束 → 計算績效指標
```

### 4.2 狀態轉移表

| 當前狀態 | 觸發條件 | 下一狀態 | 記錄 |
|---------|--------|--------|------|
| 無持倉 | Killer 上穿 Wave + price < Enter | 做多第1層 | entryTime, entryPrice |
| 無持倉 | Killer 下穿 Wave + price > Enter | 做空第1層 | entryTime, entryPrice |
| 做多持倉 | 權益回撤 ≥ 5% | 無持倉 | exitReason: 動態回撤止損 |
| 做多持倉 | 浮虧 ≤ -$100 | 無持倉 | exitReason: 硬止損 |
| 做多持倉 | 浮盈 ≥ $8 + 回撤 ≥ $1.5 | 無持倉 | exitReason: 止盈觸發 |
| 做多持倉 | 價格 ≤ 最後價格 - 網格 | 做多第N+1層 | addLayer |
| 做空持倉 | 權益回撤 ≥ 5% | 無持倉 | exitReason: 動態回撤止損 |
| 做空持倉 | 浮虧 ≤ -$100 | 無持倉 | exitReason: 硬止損 |
| 做空持倉 | 浮盈 ≥ $8 + 回撤 ≥ $1.5 | 無持倉 | exitReason: 止盈觸發 |
| 做空持倉 | 價格 ≥ 最後價格 + 網格 | 做空第N+1層 | addLayer |

---

## 第五部分：參數參考表

### 5.1 EMA 參數

| 參數名 | 預設值 | 範圍 | 說明 |
|-------|-------|------|------|
| EMA1_Period | 3 | 1-100 | Killer 快速線週期 |
| EMA2_Period | 6 | 1-100 | Wave 中速線週期 |
| EMA3_Period | 15 | 1-100 | Enter 進入線週期 |
| EMA4_Period | 30 | 1-100 | Lower 下軌線週期 |
| EMA5_Period | 60 | 1-100 | Upper 上軌線週期 |

### 5.2 馬丁參數

| 參數名 | 預設值 | 說明 |
|-------|-------|------|
| Base_Lot_Size | 0.01 | 初始開倉手數 |
| Martin_Multiplier | 1.5 | 馬丁倍率（已由 Martin_Tiers 控制） |
| MaxMartinLevels | 11 | 最大加倉層數（自動從 Martin_Tiers 讀取） |
| Martin_Tiers | JSON | 階梯式分層表（層數、乘數、間距） |
| Global_Pipstep | 50000 | 全局加倉間距（點數） |
| Point_Value | 100 | 點值（BTC=100, XAUUSD=0.01） |

### 5.3 止盈參數

| 參數名 | 預設值 | 說明 |
|-------|-------|------|
| Dollar_Start_Buy | $8.0 | 做多止盈目標金額 |
| Dollar_Start_Sell | $8.0 | 做空止盈目標金額 |
| Dollar_Trail | $1.5 | 止盈回撤金額 |

### 5.4 風控參數

| 參數名 | 預設值 | 說明 |
|-------|-------|------|
| Dollar_Loss | $100 | 硬止損金額 |
| Max_Position_Ratio | 20% | 最大持倉比例 |
| Max_Equity_Drawdown | 5% | 權益回撤止損 |
| News_Blackout_Minutes | 360 | 新聞禁開倉時間（實盤用） |

---

## 第六部分：特殊說明

### 6.1 滑點與手續費

回測中自動應用：
```
開倉滑點 = 開倉價格 × Slippage（預設 0.05%）
平倉滑點 = 平倉價格 × Slippage（預設 0.05%）
手續費 = (開倉價格 + 平倉價格) × 手數 × Commission（預設 0.05%）
```

### 6.2 均價更新

每次加倉後自動更新均價：
```
新均價 = (舊均價 × 舊手數 + 新加倉價格 × 新層手數) / (舊手數 + 新層手數)
```

### 6.3 浮盈計算

實時浮盈計算：
```
做多浮盈 = (當前價格 - 均價) × 總手數
做空浮盈 = (均價 - 當前價格) × 總手數
```

### 6.4 權益追踪

```
當前權益 = 基礎權益 + 浮盈
峰值權益 = 歷史最高權益
權益回撤 = (峰值權益 - 當前權益) / 峰值權益
```

---

## 第七部分：交易記錄字段

每筆交易記錄包含以下信息：

| 字段 | 說明 |
|------|------|
| id | 交易編號 |
| entryTime | 開倉時間戳 |
| exitTime | 平倉時間戳 |
| side | 方向（long/short） |
| entryPrice | 開倉價格 |
| exitPrice | 平倉價格 |
| size | 總手數 |
| pnl | 盈虧金額 |
| pnlPct | 盈虧百分比 |
| exitReason | 平倉原因 |
| martinLayer | 最終層數（0-based） |

---

## 版本信息

- **策略名稱**：SMA v3.00 對稱統一版
- **版本號**：3.00
- **發佈日期**：2026-07-14
- **最後更新**：2026-07-14
- **狀態**：生產版本

---

## 快速參考

### 做多入場
```
Killer(3) > Wave(6) AND price < Enter(15) → 開倉做多
```

### 做空入場
```
Killer(3) < Wave(6) AND price > Enter(15) → 開倉做空
```

### 加倉觸發
```
做多：price ≤ lastPrice - gridStep
做空：price ≥ lastPrice + gridStep
```

### 出場優先級
```
1. 權益回撤 ≥ 5% → 平倉
2. 浮虧 ≤ -$100 → 平倉
3. 浮盈 ≥ $8 + 回撤 ≥ $1.5 → 平倉
```

---

**文檔完成**
