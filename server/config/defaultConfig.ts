// server/config/defaultConfig.ts
// 🔥 V4.0 固定金本位馬丁架構

export const V4_CONFIG = {
  // ============================================================
  // 核心資金參數（固定金本位）
  // ============================================================
  Initial_Capital: 10000,           // 全局初始資金（USDT）
  Base_Lot_Size: 30,                // 首單固定金額（USDT）
  First_Order_Pct: 0.3,             // 回退用百分比（30/10000=0.3%）
  Max_Loss_Pct: 5.0,                // 硬止損：本金的 5%（= 500 USDT）

  // ============================================================
  // KAMA 參數（保持 V3.x 不變）
  // ============================================================
  KAMA_Fast_Length: 50,
  p2_fastest: 10,
  p3_slowest: 2,
  KAMA_Slow_Length: 50,
  q2_fastest: 10,
  q3_slowest: 6,

  // ============================================================
  // 馬丁加倉與分層參數
  // ============================================================
  Martin_Step_Pct: 2.0,             // 全局加倉間距（%）
  Martin_Layers: [
    { start: 1, end: 4, multiplier: 1.5 },   // 第 1-4 層：1.5 倍（常規拉低均價）
    { start: 5, end: 9, multiplier: 1.1 },   // 第 5-9 層：1.1 倍（深水區降速防守）
    { start: 10, end: 11, multiplier: 1.0 }, // 第 10-11 層：1.0 倍（極限平鋪）
  ],
  Max_Layers: 11,

  // ============================================================
  // 止盈參數
  // ============================================================
  Target_TP_Pct: 1.0,               // 移動止盈觸發點（%）
  Callback_Pct: 0.1,                // 移動止盈回撤平倉點（%）

  // ============================================================
  // 系統參數
  // ============================================================
  K_Line_Period: 30,
  Kama_Reversal_Min_Layer: 3,
};

export default V4_CONFIG;
