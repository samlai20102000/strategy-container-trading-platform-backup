# 2026-07-28 訊號盈虧、V4.0 交易與 API 帳號調查紀錄

> 本文件只記錄唯讀調查證據與待驗證假設；沒有執行下單、平倉、啟停策略或持倉修改。

## 使用者圖片證據

圖片一（訊號日誌）顯示：策略欄有 `#60007`；V6.1 的 09:12:58 平倉列顯示 `+2.74 U`，`#60007` 的 09:12:46 平倉列顯示 `+0.64 U`；20415 的 09:42:34 與 10:44:30 平倉列仍為破折號。這表示畫面不是「只有 V6.1 永遠有值」，而是只有部分平倉路徑／交易記錄成功寫入並關聯 `trades.realizedPnl`。

圖片二（OKX 歷史委託）顯示 2026-07-25 的空單開／平組合，包括：12:59:24 賣出開空、12:59:39 買入平空（約 `-0.01 USDT`）；13:09:13 賣出開空、13:09:30 買入平空（約 `-0.01 USDT`）；15:22:11 賣出開空、15:28:08 買入平空（約 `-0.01 USDT`）；15:34:16 又有一筆賣出開空，16:09:15 買入平空顯示 `+0.05 USDT / +1.06%`。圖片中的成交時間看似為 UTC+8，而資料庫 `createdAt` 為 UTC。

圖片三（策略交易卡片）目前只顯示策略名稱、交易所、交易對、倉位、槓桿、方向、下單類型、風控與輪詢資料，沒有顯示綁定的 API 帳號標籤。

## 唯讀資料庫證據

### 策略名稱 `#60007`

`strategies.id = 60007` 查無記錄，因此前端 `strategies.list` 無法找到名稱，現行 `Signals.tsx` 直接回退為 `#${id}`。但歷史訊號的 `message` 與 `rawPayload` 明確顯示這是 V3.5 路徑（例如 `[Auto] [首單開倉] V3.5 ...`、`source: v35_monitor`）。結論：名稱缺失是**策略被刪除後，訊號表沒有保存不可變的策略名稱快照**，不是單純前端字體問題。

### 訊號盈虧 JOIN

現行 `db.listSignals` 只以 `signals.id = trades.signalId`，或在 `trades.signalId IS NULL` 時以相同非空 `orderId` JOIN 單一 `trades.realizedPnl`。它沒有回傳策略名稱快照，也沒有對一個訊號的多筆／部分平倉聚合。因此只要平倉路徑未建立 trade、trade 未寫 `realizedPnl`、signalId／orderId 未接上，畫面就顯示破折號。

### 2026-07-25 策略 7（使用者稱 V4.0 KAMA+3K 動態馬丁）

策略 7 已被刪除；歷史訊號內容顯示實際執行模組為 `V3.5 Monitor`。系統內建策略定義的正式資料是：key `20415_KAMA_MARTIN_V35`、名稱 `V4.0 KAMA+3K 動態馬丁策略（百分比控倉）`、版本 `4.0.0`；因此畫面與訊息中的 V3.5 是沿用的執行模組名稱，並不代表使用者認錯策略。資料庫 UTC 與圖片 UTC+8 可逐筆對齊：

| 圖片時間（UTC+8） | DB UTC | 動作 | DB 交易證據 | 系統原因／PnL |
|---|---|---|---|---|
| 12:59:24 | 04:59:26 | 賣出開空 | trade `60003`，0.0004 BTC | V3.5 首單開倉 |
| 12:59:39 | 04:59:45 | 買入平空 | trade `60004`，0.0004 BTC | `hard_stop_loss`；訊息 `總浮虧 0.00% ≥ 0%`；DB PnL `-0.00199000` |
| 13:09:13 | 05:09:13 | 賣出開空 | trade `60005`，0.0004 BTC | V3.5 首單開倉 |
| 13:09:30 | 05:09:34 | 買入平空 | trade `60006`，0.0004 BTC | `hard_stop_loss`；訊息 `總浮虧 0.00% ≥ 0%`；DB PnL `-0.00456000` |
| 15:22:11 | 07:22:11 | 賣出開空 | trade `60007`，0.0004 BTC | V3.5 首單開倉 |
| 15:28:08 | 07:28:09 | 買入平空 | trade `60008`，0.0004 BTC | `manual`；DB 價格與 PnL 均為 NULL |

另有 12:39:25／12:40:02（UTC+8）的一組開平：DB trade `60001`／`60002`，平倉同樣被標記 `hard_stop_loss`，原因文字仍為 `總浮虧 0.00% ≥ 0%`，DB PnL `+0.02396000`。這證明設定中的止損門檻曾被解析為 **0% 且程式使用 `>= 0` 直接觸發**，導致新倉在幾十秒內立即被平；市場滑價與手續費會把接近零的毛 PnL 變成交易所顯示的小額虧損。第三筆 15:28 是手動平倉，不是同一個自動止損觸發。

目前 `v35Monitor.ts` 的 `normalizeV4MaxLossPct` 已把缺值、空字串、非數值、`<= 0` 或 `> 50` 視為無效並回退至 5%，而正式策略預設亦為 5%。這是現行保護；仍需新增回歸測試鎖定 0 不得再次成為立即平倉門檻，並在調查報告中明確指出歷史交易發生於舊／錯誤配置語意下。

### 2026-07-28 其他策略

20415 的平倉訊號文字可顯示止盈百分比，但其對應 trade 仍可能 `realizedPnl = NULL`；手動平倉 signal `150002` 對應 trade `150002` 亦為 `realizedPnl = NULL`。V6.1／V3.5 某些專用執行分支會自行計算並寫入 PnL，所以才出現部分策略有值、部分沒有值的分裂行為。

## 初步程式根因

1. `signals` 沒有不可變的 `strategyName` 快照，刪除策略後只能顯示 ID。
2. `trades` 也沒有策略名稱快照，歷史績效同樣依賴仍存在的策略列。
3. PnL 計算散落在各策略專用 CLOSE 分支；通用 webhook CLOSE、`CLOSE_ALL`、手動策略卡片平倉等路徑只建立 `reduceOnly` trade，沒有統一寫入 PnL。
4. 訊號日誌只取 JOIN 到的一筆 `trades.realizedPnl`，沒有通用聚合與明確的「非平倉＝尚未實現」狀態。
5. 策略卡片已另外查詢 `apiKeys.list`，但卡片 JSX 未把 `strategy.apiKeyId` 對應到安全的 `apiKeys.label`；可以在共用卡片一次性加入，不需暴露加密 Key／Secret。

## 已完成驗證

- 已讀取 V3.5／V4.0 hard stop 設定正規化與判斷，確認歷史錯誤配置把 0 當成「零門檻立即平倉」；現行正規化會將 0、負值、空值及非法值回退為 5%。
- 已透過 OKX bills／訂單唯讀 GET 核對三筆交易的成交、毛 PnL、費用與淨變動，全程未呼叫任何交易方法。
- 已盤點所有 `createTrade` 平倉分支並建立共用成交／PnL 真值服務，讓現有與未來策略採同一寫入契約。

## OKX 適配器的直接資料遺失證據

`OKXAdapter.queryOrderFillDetails()` 已從 `/api/v5/trade/order` 解析 `detail.pnl`，其私有回傳型別亦包含 `pnl`。但 `closePosition()`、fallback 及智能平倉分支只把 `filledPrice` 與 `filledSize` 複製到共用 `OrderResult`；共用結果型別本身也沒有 `realizedPnl` 或費用欄位。換言之，交易所已提供的平倉 PnL 在適配器邊界被丟棄，只有 V6.1／V3.5 等個別執行分支自行用持倉均價計算並傳給 `createTrade` 時才會顯示。

共用修正的真值優先序應為：交易所訂單已實現 PnL → 交易所已平倉／帳單同步結果 → 策略隔離持倉快照的可稽核毛 PnL 後備。開倉／加倉本來沒有已實現盈虧，UI 應明示「未實現」；平倉卻缺少真值時應顯示「待同步／無法核對」，不能與非平倉共用同一個破折號。

## OKX 原生帳單核對（2026-07-25，唯讀 GET）

本次以網站正式伺服器所用環境，對六個 OKX 模擬帳號逐一執行 `GET /api/v5/account/bills`。只有 **OKX模擬主帳號（apiKeyId=1）** 在圖片二時段有 BTC-USDT-SWAP 紀錄；其餘五個子帳號均為空。查詢過程沒有呼叫下單、撤單或平倉方法。

| 圖片時間（GMT+8） | 平台判定 | 開倉訂單／價／量 | 平倉訂單／成交 | 毛 PnL | 平倉費 | 平倉餘額變動 | 開倉費 | 往返淨變動 |
|---|---|---|---|---:|---:|---:|---:|---:|
| 12:59:24 → 12:59:39 | V3.5/V4.0 監控錯誤觸發 0% 最大虧損 | `3773302062098931712`／64271.2／0.04 張（0.0004 BTC） | `3773302565885173760`／三筆加權 64276.175 | -0.00199000 | -0.012855235 | -0.014845235 | -0.01285424 | **-0.027699475** |
| 13:09:13 → 13:09:30 | V3.5/V4.0 監控錯誤觸發 0% 最大虧損 | `3773321810761211904`／64284.3／0.04 張 | `3773322404607549440`／64295.7 | -0.00456000 | -0.01285914 | -0.01741914 | -0.01285686 | **-0.03027600** |
| 15:22:11 → 15:28:08 | 手動平倉（與前兩筆自動止損不同） | `3773589506543312896`／64128.7／0.04 張 | `3773601517352247296`／64130.0 | -0.00052000 | -0.01282600 | -0.01334600 | -0.01282574 | **-0.02617174** |

三筆都是**賣出開空 → 買入平空**。價格上升造成空單毛損；但倉位很小，因此真正主要成本是手續費。三筆毛損合計為 **-0.00707 USDT**，僅平倉費合計 **-0.038540375 USDT**，若連開倉費計入，三個往返淨變動合計為 **-0.084147215 USDT**。圖片的「收益」欄與 OKX 帳單顯示口徑偏向平倉側餘額變動／介面四捨五入，平台報告必須同時標示毛 PnL、費用與淨 PnL，不能只存單一不明口徑數字。

> 證據限制：歷史策略 7 已被刪除，本地 `trades` 沒有保存 API 帳號快照；本次透過六帳號逐一唯讀比對，只有 apiKeyId=1 出現與圖片時間、價位、數量完全一致的帳單，故帳號歸屬可由排他證據確定。

## 2026-07-28 20:21:55 手動平倉原生訂單核對

- 本地 signal `150002`／trade `150002` 的 orderId 為 `3782890260470939648`。逐一向六個 OKX 模擬帳號執行唯讀訂單查詢後，只有 **API 帳號 1「OKX模擬主帳號」**回傳此訂單，其餘五個帳號均無此訂單。
- OKX 原生訂單時間為 `2026-07-28T12:21:55.099Z`（使用者時區 GMT+8 即 `2026/07/28 20:21:55`），與圖片一完全對齊。
- 原生欄位為 `BTC-USDT-SWAP`、`side=buy`、`posSide=short`、`reduceOnly=true`、`state=filled`，證實它是空單平倉而不是新開多單。
- 原生成交均價 `63421.6`，成交 `0.15` 張；按合約面值換算為 `0.0015 BTC`。毛已實現 PnL `-0.35055 USDT`，平倉費 `-0.0475662 USDT`，淨已實現 PnL `-0.3981162 USDT`。
- 圖片一原「價格 63187.9」來自 signal 的解析／觸發價格，不是 OKX 最終成交均價。該 trade 已用嚴格 `id + orderId + PnL IS NULL` 條件回填交易所成交價、數量、毛損、費用、淨損及 `pnlSource=exchange`，未改動任何訂單或持倉。

## 歷史平倉 PnL 覆蓋率（migration 後）

- 已執行平倉訊號共 `63` 筆；其中 `30` 筆有 orderId、`31` 筆已有可關聯的 reduce-only trade。
- 回填上述原生訂單後，具可核對交易記錄的近期平倉已保存 PnL；仍有一筆 `2026-07-24` 的已刪除策略 #4 歷史平倉，沒有 orderId、API 帳號關聯或可證明成交資料，只能誠實標示「待同步」，不可臆造盈虧。
- 沒有可關聯 trade 的舊平倉訊號多數源於舊版本未保存交易記錄／orderId。新版修正會讓共用與策略專用平倉路徑均保存不可變名稱、實際成交、毛 PnL、費用、淨 PnL 與來源。

## 已實作的共用修正

1. `signals` 與 `trades` 現在保存不可變的 `strategyName`／`strategyKey` 快照；策略日後被改名或刪除，歷史訊號仍能顯示當時名稱，不再只剩裸 `#id`。
2. 交易所適配器的共用成交結果已加入實際成交價、成交量、毛已實現 PnL、費用、淨已實現 PnL 與來源；OKX／Bybit、通用執行器、V3.5／V4.0、V5.0、V6.1 與手動平倉均接到同一真值契約。
3. 訊號查詢會聚合同一訊號的多筆／部分平倉交易，並以不可變名稱、交易所實際成交價及淨 PnL 優先；首頁與獨立訊號頁共用同一顯示元件。
4. UI 明確區分「未實現」（開倉／加倉）、「待同步」（確認為平倉但沒有足夠歷史成交證據）與可稽核的毛／費用／淨 PnL，避免再用同一個破折號混淆三種狀態。
5. 每張現有及未來策略卡片均顯示安全 API 帳號名稱、交易所、模擬／實盤環境與帳號 ID；未綁定、已刪除或不可存取時顯示清楚後備文字，絕不回傳或顯示 API Key／Secret。

## 最終驗證紀錄

| 驗證 | 結果 |
|---|---|
| TypeScript `tsc --noEmit` | 通過，0 個型別錯誤 |
| 本輪整合／成交真值測試 | 通過，包含策略名稱快照、API 帳號安全契約、零 PnL、部分平倉聚合及成交真值優先序 |
| 隔離版本完整 Vitest | 537 項先通過；唯一既有 D2 回測因 OKX 公網 TLS 連線超過 60 秒而逾時，單獨重跑後通過，故 538 個可執行項目均有通過紀錄，另 4 項 live 測試依設計跳過 |
| 正式環境建置 | 通過；Vite 與 Node server bundle 均成功產生 |
| UI 驗證 | 預覽截圖代理連續逾時；已改以 HTTP 健康、正式 bundle、共用元件契約及靜態 JSX 檢查驗證，未把無截圖冒充為視覺通過 |
| 安全邊界 | 全程沒有下單、平倉、啟停策略或修改持倉；對交易所只執行唯讀 GET |
