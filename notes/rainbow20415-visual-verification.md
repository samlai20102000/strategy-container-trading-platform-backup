# 20415 七彩虹視覺驗收紀錄

## 2026-07-25 預覽異常診斷

生產建置與本機 HTTP 均成功，但管理預覽在 `/strategies`、`/backtest`、`/parameter-snapshots` 的第一次與第二次全頁擷取皆為純白。互動瀏覽器直接開啟最新預覽網址後，頁面標題為「策略容器化自動交易平台」，DOM 可擷取到靜態 `h1` 標題，但沒有任何 React 互動元素或路由內容。

目前證據顯示問題發生在**前端掛載／資源執行階段**，不是伺服器無回應：`http://localhost:3000/strategies` 回傳 HTTP 200 與完整 Vite HTML，伺服器已註冊所有策略並穩定監聽 3000 埠。瀏覽器日誌在重啟瞬間只記錄過 Vite 連線中斷，未留下新執行期例外，因此下一步需直接解析瀏覽器保存的 HTML、檢查 script 請求與掛載根節點，修復後再進行桌面與 390px 驗收。

## 2026-07-25 最終視覺回歸

前端掛載缺陷修復後，使用最新生產 build 建立無 HMR 靜態驗收入口，並由同一個已登入帳戶走過 `/strategies` 的真實「新增策略」流程。策略引擎清單已原位顯示 **「20415七彩虹馬丁策略（內建）」**；選取後載入的不是示意空殼，而是與正式 `Rainbow20415Config` 契約雙向綁定的完整軍規配置元件。

桌面視窗驗收確認，選取 `strategy_20415` 後對話框會由一般 `sm:max-w-lg` 即時擴展為 `sm:max-w-6xl`，身份標識、四項戰備指標、七線戰術陣列、動態馬丁區間、三道鐵幕、無縫重入與底部策略引擎均在同一垂直捲動容器內完整呈現；外層水平捲軸已移除，桌面表格只在自身必要區域保留局部 `overflow-x-auto`。

390 × 844 行動版則以正式生產 CSS、正式預設契約及同一個 `Rainbow20415ConfigPanel` 真實元件獨立渲染。首屏可見 20415 / RAINBOW / V1、實盤部署、BLIND MODE ARMED、VALID、M30 / M1、11 L、QUANTITY，以及單欄的任務時序與底倉欄位；未出現橫向裁切或桌面表格強制撐寬。這項驗收沒有呼叫建立策略、下單、平倉或任何交易 API。

| 驗收面向 | 結果 | 證據摘要 |
|---|---|---|
| 真實新增策略流程 | 通過 | 引擎清單顯示 20415七彩虹馬丁策略並可載入完整契約面板 |
| 桌面寬版 | 通過 | `sm:max-w-6xl`、外層 `overflow-x-hidden`、全部軍規扇區完整呈現 |
| 390px 行動版 | 通過 | 正式生產 CSS + 真實元件，單欄可讀且無橫向裁切 |
| 交易安全 | 通過 | 僅讀與 UI 選取，未建立策略、未發送交易動作 |
